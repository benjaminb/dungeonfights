// NOTE: MUST align with array of m_stats in Player class

namespace stat {
    enum Stat {
        str,
        dex,
        con,
        intl, // Watch out for the 'l'!
        wis,
        cha,
        ac,
        hp,
        hitDice,
        hpRolls,
        hpDie
    };
}
